# Taller-14
alumno: Carlos jose torres del rio

POO Tema central: Polimorfismo.

Consiste en poder reutilizar y var variabilidad a el codigo mediante estos casos los metodos, donde cada clase da  su propio uso al mismo metodo dotandolo de un mensaje distinto:

Actividad 1:

Se pidio una clase Figura con 2 clases derivdad y un metodo.
    -Clase Circulo con su propia variación de Calcular area.
    -Clase Recangulo con su propia variación de calcular area.
    -Metodo calculararea (Imprime un mensaje referente)

Se pidio una clase de prubea para mostrar el polimorfismo.
    -Se hizo uso de una clase para mostrar atraves de System out print los diferentes mensajes de cada clase.
    -Se hizo uso del polimorfismo al dar al mismo metodo un significado variable del origen del objeto, es decir segun la clase.
    -Se hizo un objeto adicional para mostrar el  mensaje dentro de la clase padre y mostrar  como un mismo metodo puede ser reutilizado de multiples formas

Actividad 2:

Se pidio una clase persona con un metodo y 2 clases  derivadas.
    -Meotodo presentarse con un mensaje referente.
    Classe profesor con su propia sobreescritura de presentarse.
    Clase Estudiante con su propia sobreescritura  de presentarse.

Se crep una clase para invocar los mesajes de cada variación del metodo.
    -Se creo un objeto para cada clase y se invoco el mensaje del metodo atraves  de estos.
Los mensajes deben ser:
    -Hola
    -El profesor dice hola.
    -El estudiantes dice hola.

Actividad 3:

Se pidio una clase vehiculo con un metodo y 2 clases derivadas.
    -Se creo la clase bicicleta
    -Se creo la clase coche
    -Se creo el metodo mover.
Se sobreescribe la funcionalidad del metodo mover en el interio de las clases bicicleta y coche.

se crea una clase prueba. Donde se crean objetos de bicicleta y coche para mostrar el polimorfismo del metodo mover a su vez que se muestra el original con la clase vehiculo usando un objeto de esta.

Actividad 4:

Se pidio una clase nombrada Error con 2 sub clases con un nombre similar.
    -Una de las clases posee un metodo que no existe en la clase padre.
    -Una de las clases sobreescribe un metodo sin hacer ningun cambio.
Se intenta invocar un metodo que no existe en la clase padre usando main.

